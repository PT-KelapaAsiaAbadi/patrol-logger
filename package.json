{
  "name": "guard-patrol-prototype",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "QR-code guard patrol prototype: a guard app and a staff app from one repository, with the backend simulated in the browser.",
  "scripts": {
    "start": "node tools/python.mjs -m http.server 8000 --bind 127.0.0.1",
    "lint": "eslint apps shared dev tools",
    "typecheck": "tsc -p jsconfig.json",
    "format": "prettier --write \"apps/**/*.{js,css,html}\" \"shared/**/*.{js,css}\" \"dev/**/*.{js,html}\" \"tools/**/*.mjs\"",
    "test": "node tools/python.mjs tests/make_fake_camera.py tests/fake_camera.y4m && node tools/python.mjs tests/e2e.py",
    "build": "node tools/build.mjs",
    "build:demo": "node tools/build-demo.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.4.0",
    "esbuild": "^0.23.0"
  }
}
